将一个数组里的元素转换成一个字符串方法：
例
13 14 14 15->"13.14.14.15"
        int len = 0;
        for (int i = 0; i < 4; i++) {
            if (i != 0)
                tep1[len++] = '.';
            len += sprintf(tep1 + len, "%d", tep[i]);
        }
        tep1[len] = '\0';
这个方法很好！
